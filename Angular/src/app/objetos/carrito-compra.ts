export interface CarritoCompra {
    id: number,
    descripcion: string;
    nombre: string,
    imagen: string,
    precio: number,
    disponible: number,
    subtotal: number
    cantidad: number
}